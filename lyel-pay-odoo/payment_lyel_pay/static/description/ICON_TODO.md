# icon.png to add

The Odoo App Store expects an `icon.png` at this path, 256x256 pixels,
PNG with transparency. Drop the Lyel Pay logo here as `icon.png` before
publishing.

Source files (logo, favicons) live in the `packages/design-system` and
`packages/checkout-ui` directories of the monorepo.
